# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'Demo_RTC' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Demo_RTC

  pod 'GoogleWebRTC'
  #pod 'WebRTC-lib'
  pod 'Starscream', :inhibit_warnings => true
  pod 'CocoaLumberjack', :inhibit_warnings => true
  pod 'MPMessagePack', :inhibit_warnings => true

  target 'Demo_RTCTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Demo_RTCUITests' do
    # Pods for testing
  end

end
